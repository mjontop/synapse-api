# MongoDB URI (replace with your actual MongoDB URI)
MONGODB_URI="your_mongo_URI/URL"

# Database name
DB_NAME="your_database_name"

# Server address (replace with your actual server address)
ADDRESS="example.com:1234"

# JWT secret key (replace with a strong, unique secret)
JWT_SUPER_SECRET="your_jwt_secret_here"
